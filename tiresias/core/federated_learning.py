
def gradients(model, weights, data):
    """
    Given the model, weights, and a data point, compute the gradient.
    """
    return NotImplementedError()

def aggregate(model, weights):
    """
    Given the model and weights, compute the gradient update.
    """
    return NotImplementedError()
